# OSX
.DS_Store

# Xcode
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata
*.xccheckout
*.moved-aside
DerivedData
*.hmap
*.ipa
*.xcuserstate
ios/.xcode.env.local
ios/Pods/
ios/Podfile.lock

# Android
android/app/build/
android/app/.cxx/
android/build/
android/.gradle/
android/local.properties
# Signing keystores — never commit. Master copies live in 1Password,
# base64'd into ANDROID_KEYSTORE_BASE64 secret for CI.
android/app/release.keystore
android/app/*.keystore
android/app/*.jks
# Apple Sign In / App Store Connect API keys
*.p8
*.iml

# Node / Metro
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
.metro-health-check*

# RN
.bundle/

# Env
.env
.env.local
.env.development
.env.production
# Note: src/lib/config.ts used to be ignored when it held hardcoded
# values; it's now a generic file that reads from @env, so it's safe
# (and required) to commit.

# Sentry
.sentryclirc

# Store screenshots — raw device captures + generated store-sized assets.
# The generator script (scripts/generate-store-screenshots.ts) and the
# SCREENSHOTS.md guide are committed; the images themselves are not.
store-assets/raw-screens/
store-assets/screenshots/
# Downloaded CI build artifacts (AABs) — never commit; pull fresh from the run.
store-assets/aab/
